name: "WINDOWS MMO (AUTO GÕ TOKEN + RDP)"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-rdp:
    name: "Traffmonetizer Auto Node"
    runs-on: ubuntu-latest
    timeout-minutes: 360

    env:
      WINDOWS_USERNAME: "user"
      WINDOWS_PASSWORD: "ngoc0102@"

    steps:
      - name: Prepare workspace and OEM scripts
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo apt-get update > /dev/null 2>&1
          sudo mkdir -p /mnt/windows_data
          sudo chmod 777 /mnt/windows_data
          mkdir -p oem

          # === SCRIPT NHÚNG SÂU ===
          cat > oem/setup-system.ps1 <<'POWERSHELL'
          $ErrorActionPreference = 'SilentlyContinue'
          $ProgressPreference = 'SilentlyContinue'
          
          # TẢI WARP VÀ TRAFFMONETIZER
          Invoke-WebRequest -Uri "https://1111-releases.cloudflareclient.com/win/latest" -OutFile "C:\warp.msi" -UseBasicParsing
          Invoke-WebRequest -Uri "https://data.traffmonetizer.com/downloads/Traffmonetizer.exe" -OutFile "C:\traff.exe" -UseBasicParsing
          
          # CÀI ĐẶT
          Start-Process "C:\traff.exe" -ArgumentList "/S" -Wait
          Start-Process msiexec.exe -ArgumentList "/i C:\warp.msi /qn" -WindowStyle Hidden -Wait
          
          Start-Sleep 5
          & "C:\Program Files\Cloudflare\Cloudflare WARP\warp-cli.exe" --accept-tos registration new | Out-Null
          Start-Sleep 2
          & "C:\Program Files\Cloudflare\Cloudflare WARP\warp-cli.exe" --accept-tos mode warp | Out-Null
          Start-Sleep 2
          & "C:\Program Files\Cloudflare\Cloudflare WARP\warp-cli.exe" --accept-tos connect | Out-Null

          # === TẠO BÀN TAY MA THUẬT (AUTO GÕ PHÍM) ===
          $vbs = @(
              'Set WshShell = WScript.CreateObject("WScript.Shell")'
              'WScript.Sleep 10000'
              'WshShell.AppActivate "Traffmonetizer"'
              'WScript.Sleep 2000'
              'WshShell.SendKeys "6HN0okse0Lcd2aIrgEB2ZY6hIsEIhl1QepxAh78+PLA="'
              'WScript.Sleep 1000'
              'WshShell.SendKeys "{ENTER}"'
          ) -join [Environment]::NewLine
          Set-Content -Path "C:\auto_token.vbs" -Value $vbs -Encoding ASCII

          # === ĐẶT BẪY KHỞI ĐỘNG ===
          $startupDir = "$env:ProgramData\Microsoft\Windows\Start Menu\Programs\StartUp"
          $cmd = @(
              '@echo off'
              'timeout /t 5 /nobreak >nul'
              'start /min "" "C:\Program Files\Cloudflare\Cloudflare WARP\Cloudflare WARP.exe"'
              'timeout /t 2 /nobreak >nul'
              ':: Goi App len man hinh'
              'if exist "C:\Program Files\Traffmonetizer\Traffmonetizer.exe" start "" "C:\Program Files\Traffmonetizer\Traffmonetizer.exe"'
              'if exist "C:\Program Files (x86)\Traffmonetizer\Traffmonetizer.exe" start "" "C:\Program Files (x86)\Traffmonetizer\Traffmonetizer.exe"'
              'if exist "%LOCALAPPDATA%\Programs\Traffmonetizer\Traffmonetizer.exe" start "" "%LOCALAPPDATA%\Programs\Traffmonetizer\Traffmonetizer.exe"'
              ':: Goi con ma ra go phim'
              'cscript //nologo C:\auto_token.vbs'
              ':: Xoa dau vet'
              'del "C:\auto_token.vbs"'
              'del "%~f0"'
          ) -join [Environment]::NewLine
          Set-Content -Path "$startupDir\auto-bot.cmd" -Value $cmd -Encoding ASCII

          # Dọn rác
          Remove-Item -Path "C:\warp.msi" -Force
          Remove-Item -Path "C:\traff.exe" -Force
          POWERSHELL

          cat > oem/install.bat <<'BAT'
          @echo off
          powershell -WindowStyle Hidden -NoProfile -ExecutionPolicy Bypass -File "C:\oem\setup-system.ps1"
          shutdown /r /t 10
          BAT

      - name: Generate docker-compose
        shell: bash
        run: |
          cat > docker-compose.yml <<EOF
          version: "3.9"
          services:
            windows:
              image: dockurr/windows
              container_name: windows_rdp
              privileged: true
              environment:
                VERSION: "10"
                USERNAME: "${WINDOWS_USERNAME}"
                PASSWORD: "${WINDOWS_PASSWORD}"
                RAM_SIZE: "4G"
                CPU_CORES: "4"
                DISK_SIZE: "64G"
              devices:
                - /dev/kvm
              ports:
                - "3389:3389"
                - "8006:8006"
              volumes:
                - /mnt/windows_data:/storage
                - ./oem:/oem
          EOF

      - name: Boot Windows
        shell: bash
        run: |
          set -Eeuo pipefail
          docker compose up -d > /dev/null 2>&1

      - name: Start tunnels
        shell: bash
        run: |
          set -Eeuo pipefail
          wget -q https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-linux-amd64.tar.gz
          tar -xzf kami-tunnel-linux-amd64.tar.gz > /dev/null 2>&1
          chmod +x kami-tunnel
          
          mkdir -p tunnel_rdp tunnel_web
          cp kami-tunnel tunnel_rdp/
          cp kami-tunnel tunnel_web/
          
          cd tunnel_rdp
          nohup ./kami-tunnel 3389 > rdp.log 2>&1 &
          
          cd ../tunnel_web
          nohup ./kami-tunnel 8006 > web.log 2>&1 &

      - name: Print connection info
        shell: bash
        run: |
          set -Eeuo pipefail

          RDP_IP=""
          RDP_RETRY=0
          while [ -z "$RDP_IP" ] && [ $RDP_RETRY -lt 60 ]; do
            if [ -f tunnel_rdp/kami_tunnel.txt ]; then
              RDP_CONTENT=$(head -1 tunnel_rdp/kami_tunnel.txt 2>/dev/null || true)
              if [[ "${RDP_CONTENT:-}" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+:[0-9]+$ ]]; then
                RDP_IP="$RDP_CONTENT"
                break
              fi
            fi
            sleep 2
            RDP_RETRY=$((RDP_RETRY + 1))
          done

          WEB_IP=""
          WEB_RETRY=0
          while [ -z "$WEB_IP" ] && [ $WEB_RETRY -lt 60 ]; do
            if [ -f tunnel_web/kami_tunnel.txt ]; then
              WEB_CONTENT=$(head -1 tunnel_web/kami_tunnel.txt 2>/dev/null || true)
              if [[ "${WEB_CONTENT:-}" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+:[0-9]+$ ]]; then
                WEB_IP="$WEB_CONTENT"
                break
              fi
            fi
            sleep 2
            WEB_RETRY=$((WEB_RETRY + 1))
          done

          echo "====================================================="
          echo "🔥 AUTO BOT: TRAFFMONETIZER (CÓ GÓI GÕ PHÍM AUTO) 🔥"
          echo "🖥️ DÙNG CHO APP RDP  : ${RDP_IP}"
          echo "🌐 DÙNG CHO WEB      : http://${WEB_IP}"
          echo "👤 USERNAME         : ${WINDOWS_USERNAME}"
          echo "🔑 PASSWORD         : ${WINDOWS_PASSWORD}"
          echo "====================================================="

      - name: Keep runner alive
        shell: bash
        run: |
          for i in {1..72}; do
            echo "May chu van song ($((i * 5)) phut / 360 phut) - AUTO BOT DANG CAY TIEN..."
            sleep 300
          done
